import { makeStyles } from "@material-ui/core/styles";
export const useStyles = makeStyles(theme => ({
   gridStyle:{
    paddingLeft: "9%", maxHeight: 450, overflow: "scroll"
   },
   noappData:{
    textAlign: "center", marginTop: 170
   }
  }));